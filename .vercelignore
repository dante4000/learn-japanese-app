node_modules
.vercel
tests
tools
docs
data/gen
.DS_Store
*.log
# This project deploys only the kana trainer at the repo root, and its output
# directory is that root, so anything uploaded is served publicly. Every app in
# apps/ has its own Vercel project, nothing here links to /apps/, and the Next
# apps can't run from raw source anyway. Shipping them cost 640MB per deploy.
apps/
